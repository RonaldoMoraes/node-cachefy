export const DEFAULT_TTL = 3600;
export const DEFAULT_ERROR_MODE = 'strict' as const;
export const DEFAULT_DRIVER = 'memory' as const;
export const DEFAULT_MAX_RETRIES = 3;
export const DEFAULT_RETRY_DELAY = 1000; // 1 second
export const DEFAULT_MAX_SIZE = 1000; // for memory driver
